*.tfstate
*.tfstate.backup
# `??????` matches mktemp residue only, so terraform.tfvars.example stays tracked
terraform.tfstate.??????
terraform.tfvars
terraform.tfvars.??????
tfplan
.terraform/
.remote-*
!*.age
