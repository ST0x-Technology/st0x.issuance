# Database configuration
DATABASE_MAX_CONNECTIONS=5

# Blockchain configuration (required for minting/burning)
RPC_URL=${RPC_URL}
PRIVATE_KEY=${PRIVATE_KEY}
VAULT_ADDRESS=${VAULT_ADDRESS}
BOT_WALLET=${BOT_WALLET}

# Authentication configuration
# API key for authenticating inbound Alpaca requests (REQUIRED, min 32 chars)
# Generate with: openssl rand -hex 32
ISSUER_API_KEY=${ISSUER_API_KEY}

# Optional: IP whitelist for issuer endpoints (comma-separated CIDR notation)
# If not set or commented out, IP whitelisting is DISABLED (not recommended for production)
# Uncomment and set when you receive Alpaca's IP ranges:
# ALPACA_IP_RANGES=1.2.3.0/24,5.6.7.8/32

# Alpaca API configuration (required for ITN integration)
ALPACA_API_BASE_URL=https://broker-api.alpaca.markets
ALPACA_ACCOUNT_ID=${ALPACA_ACCOUNT_ID}
ALPACA_API_KEY=${ALPACA_API_KEY}
ALPACA_API_SECRET=${ALPACA_API_SECRET}
ALPACA_CONNECT_TIMEOUT_SECS=10
ALPACA_REQUEST_TIMEOUT_SECS=30

# Optional: HyperDX observability integration
# Enables trace export to HyperDX for real-time monitoring and debugging
# If not set, the bot runs normally with console-only logging
# Get your API key from https://www.hyperdx.io/
HYPERDX_API_KEY=${HYPERDX_API_KEY}

# Optional: HyperDX service name (defaults to "st0x-issuance" if not set)
HYPERDX_SERVICE_NAME=st0x-issuance

# Optional: Log level (defaults to "debug" if not set)
# Valid values: trace, debug, info, warn, error
LOG_LEVEL=debug

# Optional: Override default log filters
# If not set, defaults to "st0x_issuance={LOG_LEVEL}"
RUST_LOG=${RUST_LOG}
